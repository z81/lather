{
  "name": "lori",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --minify --sourcemap=external --outfile=dist/index.js",
    "watch": "esbuild src/index.ts --bundle --watch --sourcemap=external --outfile=dist/index.js",
    "dev": "concurrently \"yarn watch\" \"nodemon dist/index.js\"",
    "debug": "concurrently \"yarn watch\" \"nodemon --inspect=0.0.0.0 dist/index.js\"",
    "test": "jest",
    "example:watch": "esbuild src/examples/a/index.ts --bundle --watch --sourcemap=external --outfile=dist/example.js",
    "example": "concurrently \"yarn example:watch\" \"nodemon dist/example.js\"",
    "test:watch": "jest --watchAll --collectCoverage",
    "docs": "typedoc src/index.ts --excludePrivate --excludeProtected --excludeInternal"
  },
  "jest": {
    "transform": {
      "^.+\\.tsx?$": [
        "esbuild-jest",
        {
          "sourcemap": true,
          "loaders": {
            ".test.ts": "ts"
          }
        }
      ]
    },
    "coveragePathIgnorePatterns": [
      "/node_modules/",
      "/tests/"
    ]
  },
  "devDependencies": {
    "@types/jest": "^26.0.24",
    "@types/node": "^15.12.5",
    "concurrently": "^6.2.0",
    "esbuild": "^0.12.12",
    "esbuild-jest": "^0.5.0",
    "jest": "^27.0.6",
    "nodemon": "^2.0.9",
    "typedoc": "^0.21.4",
    "typescript": "^4.3.4"
  }
}
